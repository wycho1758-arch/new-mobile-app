# OpenClaw Generated-Agent Package Evidence

Command:

```sh
npm run test:openclaw
```

Result:

- PASS: native Codex skill sources package into `.generated/openclaw-skill-packages`.
- PASS: package simulation preserves one top-level slug per skill.
- PASS: each simulated installed package contains `SKILL.md`.
- PASS: simulated install path is `/workspace/skills/<slug>`.

Validated source artifacts:

- `.agents/skills/mobile-app-dev-workflow`
- `.agents/skills/mobile-backend-api-integrator-workflow`
