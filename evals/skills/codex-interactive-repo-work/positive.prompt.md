$codex-interactive-repo-work A bootstrapped OpenClaw role pod has a `codex-role-workflow/v1` result requiring repo edits. Use `/workspace/projects/Wondermove-Inc/new-mobile-app` as workdir, launch PTY Codex interactive mode through `/workspace/codex-hooks/codex-run` when compatible or raw `codex` with a recorded reason, instruct Codex to use `$wm`, stay within approved scope and paths, write tests first, then require post-session `git diff`, validation output, final read-only `.codex/agents` reviewer evidence, `git status --short`, branch/PR handoff, and no self-approval.
