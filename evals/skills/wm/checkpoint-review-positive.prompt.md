$wm Plan and execute a Codex runtime change. The approved plan must define step-by-step checkpoint review boundaries before implementation starts. Each checkpoint review must include the approved plan, checkpoint diff, command output, evidence path, remaining plan impact, and the read-only reviewer verdict. Failed or blocked checkpoint review findings must be addressed before the next checkpoint proceeds. Also inspect `.codex/agents/**` read-only before reporting the final wm modification plan.
