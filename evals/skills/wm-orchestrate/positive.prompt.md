$wm-orchestrate Resolve the next allowed work-unit action from a committed status.json, run node scripts/work-unit-next.mjs for the current WM_ROLE, follow only the returned allowed_actions, stop on any blocked_reasons, route to the named required_reviewer before continuing, halt on required_human_gate until a matching approved human-gate/v1 decision is committed, and never mutate external platforms or self-approve reviewer evidence.
