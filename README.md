# new-mobile-app

WonderMove mobile app template runtime.

Start with `AGENTS.md` for mandatory agent rules, `PROJECT_ENVIRONMENT.md` for
current runtime facts, and `REPO_OPERATIONS.md` for repo-wide operating policy.
Use `docs/SETUP.md`, `docs/CREDENTIALS.md`, `DESIGN.md`, and
`mobile-app-dev-team/` for setup, credential handling, design SoT, and role/team
process detail.
