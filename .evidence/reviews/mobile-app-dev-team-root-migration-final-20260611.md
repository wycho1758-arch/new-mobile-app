# mobile-app-dev-team Root Migration Final Evidence

Date: 2026-06-11
Scope: promote current managed docs from `team-doc/mobile-app-dev-team/` to root
`mobile-app-dev-team/`, remove live `team-doc/`, and keep runtime gates passing.

## Layout

```text
test ! -e team-doc && test -d mobile-app-dev-team
root-doc-layout-ok
```

`CLAUDE.md` was initially moved out of the repository to:

```text
/tmp/CLAUDE.md.new-mobile-app.backup-20260611070646
```

Later headless review regenerated root Claude transient artifacts. The runtime
policy was updated so this is allowed as long as the generated paths remain
ignored and outside active Codex runtime ownership. The root ignore policy covers:

```text
CLAUDE.md
.claude/
.claude-state/
```

Verification with regenerated `CLAUDE.md` present:

```text
test -e CLAUDE.md && printf 'CLAUDE_PRESENT_FOR_TEST=1\n'
node scripts/validate-runtime-artifacts.mjs

CLAUDE_PRESENT_FOR_TEST=1
Validated 12 skills, 13 agents, and 4 hook events.
exit=0
```

## Validator Evidence

```text
node scripts/validate-repo-operations.mjs
Validated repo operations policy ownership.

node scripts/validate-team-doc.mjs
Validated current mobile-app-dev-team managed docs.

node scripts/validate-team-doc-archive.mjs
Validated team-doc archive/reference corpus from root archive files: 71 source files, 32 structured files.

node scripts/validate-project-environment.mjs --self-test && node scripts/validate-project-environment.mjs
Validated project environment fixtures.
Validated project environment drift checks.

git diff --check
exit=0
```

## Runtime Gate

```text
pnpm run test:runtime
Validated 12 skills, 13 agents, and 4 hook events.
Codex headless review helper self-test passed.
Validated repo operations policy ownership.
Validated current mobile-app-dev-team managed docs.
Validated work-unit status fixtures.
Validated work-unit status artifacts.
Validated work-unit next-action resolver fixtures.
Validated EAS evidence ingest fixtures.
Validated project environment fixtures.
Validated project environment drift checks.
Validated evidence hygiene fixtures.
Validated evidence hygiene artifacts.
Passed 44 hook fixture tests.
exit=0
```

## Local Harness And Build/Test Evidence

```text
pnpm run test:local-harness
clean-tree-guard self-test passed
codex-preflight self-test passed
codex-preflight accepted /opt/homebrew/bin/codex (codex-cli 0.137.0)
pnpm run test:runtime passed
pnpm turbo run lint test:
  Tasks: 7 successful, 7 total
  @template/contracts build passed
  @template/contracts lint passed
  @template/contracts test passed: 1 test
  mobile lint passed
  mobile test passed: 2 suites, 5 tests
  @template/api lint passed
  @template/api test passed: 1 file, 2 tests
local harness self-test all passed
local harness all passed
exit=0
```

## Reviewer Checkpoint

Initial final reviewer returned GO with one Low finding: standalone future edits to
`scripts/validate-repo-operations.mjs` or `scripts/validate-team-doc.mjs` did not
independently trigger conditional `test:local-harness`.

Remediation:

- Added `validate-repo-operations`, `validate-team-doc`, and
  `validate-team-doc-archive` to `.github/workflows/quality-gate.yml` runtime
  change detection.
- Documented those scripts in `PROJECT_ENVIRONMENT.md` CI path triggers.
- Added drift assertions in `scripts/validate-project-environment.mjs`.
- Reran targeted validators, `pnpm run test:runtime`, and
  `pnpm run test:local-harness`; all passed.

## Claude Transient Artifact Remediation

After final reviewer execution, `CLAUDE.md`, `.claude/`, and `.claude-state/`
were regenerated by local/headless tooling. Remediation:

- Kept `CLAUDE.md`, `.claude/`, and `.claude-state/` in `.gitignore`.
- Updated `scripts/validate-runtime-artifacts.mjs` to permit regenerated root
  Claude transient artifacts only when `.gitignore` covers them.
- Updated `PROJECT_ENVIRONMENT.md` to document that these paths are ignored
  transient files, not active Codex runtime artifacts.

Post-remediation verification:

```text
node scripts/validate-runtime-artifacts.mjs
Validated 12 skills, 13 agents, and 4 hook events.

node scripts/validate-project-environment.mjs --self-test && node scripts/validate-project-environment.mjs
Validated project environment fixtures.
Validated project environment drift checks.

pnpm run test:runtime
Validated 12 skills, 13 agents, and 4 hook events.
Codex headless review helper self-test passed.
Validated repo operations policy ownership.
Validated current mobile-app-dev-team managed docs.
Validated work-unit status fixtures.
Validated work-unit status artifacts.
Validated work-unit next-action resolver fixtures.
Validated EAS evidence ingest fixtures.
Validated project environment fixtures.
Validated project environment drift checks.
Validated evidence hygiene fixtures.
Validated evidence hygiene artifacts.
Passed 44 hook fixture tests.
exit=0

pnpm run test:local-harness
clean-tree-guard self-test passed
codex-preflight self-test passed
codex-preflight accepted /opt/homebrew/bin/codex (codex-cli 0.137.0)
pnpm run test:runtime passed
pnpm turbo run lint test:
  Tasks: 7 successful, 7 total
  @template/contracts build passed
  @template/contracts lint passed
  @template/contracts test passed: 1 test
  mobile lint passed
  mobile test passed: 2 suites, 5 tests
  @template/api lint passed
  @template/api test passed: 1 file, 2 tests
local harness self-test all passed
local harness all passed
exit=0
```
